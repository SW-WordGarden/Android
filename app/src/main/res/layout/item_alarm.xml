<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/cl_list_weekly_quiz_check"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/margin_medium"
        android:layout_marginTop="@dimen/margin_extra_small"
        android:layout_marginEnd="@dimen/margin_medium"
        android:layout_marginBottom="@dimen/margin_extra_small"
        android:background="@drawable/bg_rec_stroke_radius_20"
        android:padding="@dimen/padding_large">

    <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tv_alarm_data"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="00/00"
            android:textSize="@dimen/text_smaller"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

    <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tv_alarm_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/alarm_check_quiz"
            android:textSize="@dimen/text_caption"
            app:layout_constraintStart_toStartOf="@+id/tv_alarm_data"
            app:layout_constraintTop_toBottomOf="@+id/tv_alarm_data"
            android:layout_marginTop="@dimen/margin_extra_small" />
</androidx.constraintlayout.widget.ConstraintLayout>