<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WordGarden" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.WordGarden" parent="Base.Theme.WordGarden" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/light_khaki</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/inset_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.WordGarden</item>
    </style>
</resources>